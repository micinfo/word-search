{
  "buildCommand": "npm install && npm run build:shared && cd packages/memorygamev2 && npm run build",
  "installCommand": "npm install",
  "outputDirectory": "packages/memorygamev2/build",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
