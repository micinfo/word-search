{
  "buildCommand": "npm install && npm run build:shared && cd packages/matchgame && npm run build",
  "installCommand": "npm install",
  "outputDirectory": "packages/matchgame/build",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
