{
  "buildCommand": "npm install && npm run build:shared && cd packages/suganon && npm run build",
  "installCommand": "npm install",
  "outputDirectory": "packages/suganon/build",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
