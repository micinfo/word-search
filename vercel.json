{
  "buildCommand": "npm install && npm run build:shared && cd packages/kcab && npm run build",
  "installCommand": "npm install",
  "outputDirectory": "packages/kcab/build",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
