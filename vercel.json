{
  "buildCommand": "npm install && npm run build:shared && cd packages/slackscheduler && npm run build",
  "installCommand": "npm install",
  "outputDirectory": "packages/slackscheduler/build",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
